# RAG with LlamaIndex - Dependencies
# Python 3.10+ required

# Core LlamaIndex packages
# Use llama-index >= 0.10.0 for best compatibility
llama-index>=0.10.0
llama-index-core>=0.10.0
llama-index-readers-file>=0.1.0

# Embedding support
llama-index-embeddings-huggingface>=0.1.0

# Document processing
pypdf>=4.0.0
pydantic>=2.0.0

# Machine Learning / NLP
# PyTorch - install separately for CUDA support
# pip install torch --index-url https://download.pytorch.org/whl/cu121
torch>=2.0.0
transformers>=4.35.0
sentence-transformers>=2.2.0

# FAISS for vector search
faiss-cpu>=1.7.4
# faiss-gpu>=1.7.4  # Uncomment if using GPU with CUDA

# Data processing
numpy>=1.24.0
pandas>=2.0.0

# Optional: Quantization (for Mistral 4-bit)
# bitsandbytes>=0.41.0
# accelerate>=0.25.0

# Utilities
tqdm>=4.65.0
pyyaml>=6.0
python-dotenv>=1.0.0

# Visualization
matplotlib>=3.7.0
seaborn>=0.12.0

# Jupyter support (if using notebooks)
jupyter>=1.0.0
ipykernel>=6.0.0

# Development / Testing
pytest>=7.0.0
